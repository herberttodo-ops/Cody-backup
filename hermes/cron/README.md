# Cron Jobs

Cron jobs are managed via the `cronjob` tool.

To restore after backup:
1. List current cron jobs with: `hermes cronjob list`
2. Recreate each job from the summaries below

## Current Jobs
